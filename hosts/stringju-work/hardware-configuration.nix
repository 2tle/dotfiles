# PLACEHOLDER: On stringju-work, replace this entire file with
# /etc/nixos/hardware-configuration.nix generated for THAT PC.
# Set the actual machine's bootloader in configuration.nix too.
{ ... }:
{
  assertions = [
    {
      assertion = false;
      message = "stringju-work: replace hosts/stringju-work/hardware-configuration.nix with the target PC's generated hardware configuration before building";
    }
  ];
}
